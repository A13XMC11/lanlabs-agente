# AgentKit — Variables de entorno
# Copia este archivo como .env y llena tus datos:
#   cp .env.example .env
#
# Claude Code te ayuda a llenar estos valores durante el setup.
# NUNCA subas el archivo .env a GitHub.

# ── Anthropic API ──────────────────────────────────────────
# Obtener en: https://platform.anthropic.com/settings/api-keys
ANTHROPIC_API_KEY=

# ── Proveedor de WhatsApp ──────────────────────────────────
# Opciones: whapi | meta | twilio
WHATSAPP_PROVIDER=whapi

# ── Whapi.cloud (si WHATSAPP_PROVIDER=whapi) ──────────────
# Obtener en: https://whapi.cloud (plan gratis disponible)
WHAPI_TOKEN=

# ── Meta Cloud API (si WHATSAPP_PROVIDER=meta) ────────────
# Configurar en: https://developers.facebook.com
# META_ACCESS_TOKEN=
# META_PHONE_NUMBER_ID=
# META_VERIFY_TOKEN=agentkit-verify

# ── Twilio (si WHATSAPP_PROVIDER=twilio) ──────────────────
# Configurar en: https://twilio.com
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# ── Servidor ──────────────────────────────────────────────
PORT=8000
ENVIRONMENT=development

# ── Base de datos ─────────────────────────────────────────
# Local (SQLite):
DATABASE_URL=sqlite+aiosqlite:///./agentkit.db
# Produccion (PostgreSQL en Railway):
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname

# ── Redis (Buffering de mensajes múltiples) ───────────────
# Necesario para agrupar múltiples mensajes de WhatsApp en una sola respuesta
# Local: redis://localhost:6379
# Produccion (Railway/Upstash): redis://:password@host:port
REDIS_URL=redis://localhost:6379

# ── Configuración de Buffering ─────────────────────────────
# Timeout para esperar más mensajes (en milisegundos)
# 2500ms = espera 2.5 segundos antes de responder
BUFFER_TIMEOUT_MS=2500

# Edad máxima del buffer (previene mezclar tópicos lejanos)
# 300000ms = 5 minutos
MAX_BUFFER_AGE_MS=300000

# ── Transferencia a Operador Humano ──────────────────────
# Número personal del operador para notificaciones
# Recibirá alertas cuando un cliente pida atención humana
OPERATOR_PHONE=593996979291

# Horas que el bot permanece pausado antes de auto-reactivarse
# Si el operador olvida reanudar, el bot se reactiva automáticamente
HANDOFF_TTL_HOURS=2

# ── Google Calendar (Agendador de Citas) ──────────────────
# Ruta al archivo JSON del Service Account
# Descargar desde Google Cloud Console
GOOGLE_CREDENTIALS_FILE=credentials/google-service-account.json

# ID del calendario (generalmente el email del calendario compartido)
# Ej: alexander777800@gmail.com
GOOGLE_CALENDAR_ID=

# Duración de las reuniones en minutos
MEETING_DURATION_MINUTES=60

# Buffer entre reuniones (espacio libre para descanso)
MEETING_BUFFER_MINUTES=15

# Número de slots a ofrecer al agendar
SLOTS_TO_OFFER=3
